# douyin-liked-analyzer 分步开发计划

## 摘要

当前项目目标是做一个**本地优先、确定性 pipeline 驱动**的视频内容提取与总结工具：先用 `ffprobe/ffmpeg/faster-whisper/OCR` 提取可验证证据，再用 GPT 做压缩和总结。第一阶段不做 Web UI、多 agent、向量数据库、批量平台或逐帧视觉分析。

当前仓库状态：

- 已初始化 Git，当前分支 `master`。
- `README.md` 已暂存。
- `.gitignore`、`docs/`、`video_summarizer_architecture_plan.md` 未暂存。
- `video/` 和 `yt-dlp.exe` 已被 `.gitignore` 忽略。
- 当前样例素材里有 `video-only` 和 `audio-only` 文件，所以 MVP 必须先支持流检测，不能假设每个 `.mp4` 都同时有音频和视频。

默认技术选择：

- Python CLI：`Typer`
- 元数据/音视频处理：`ffprobe` / `ffmpeg`
- 转录：`faster-whisper`
- OCR：PaddleOCR，但作为可选/可降级模块
- 测试：`pytest`
- 第一阶段输出：JSON + SRT + Markdown，不做 HTML

## 分步计划

| 步骤 | 开发目标 | 测试验收 | 推荐指令 |
|---|---|---|---|
| 0 | 整理 Git 基线：确认 `.gitignore`、`README.md`、`docs/PLAN.md` 进入版本管理；不提交 `video/`、`yt-dlp.exe`、输出目录。 | `git status --short` 只显示应提交的文档和配置；无大视频和 `.exe` 被暂存。 | `/goal 整理当前 Git 基线，只提交 README.md、.gitignore 和 docs/PLAN.md，不提交 video/、yt-dlp.exe 或生成产物，并汇报 git status。` |
| 1 | 创建最小 Python 项目骨架：`pyproject.toml`、`src/video_summarizer/`、`tests/`、Typer CLI 入口。 | 能运行 `python -m video_summarizer --help` 或等价 CLI；测试命令能启动。 | `/goal 创建最小 Python CLI 项目骨架，使用 Typer，暂不实现视频处理逻辑，只提供可运行的 help 和测试入口。` |
| 2 | 实现 `inspect`：对单个本地媒体文件运行 `ffprobe`，生成 `metadata.json`。 | 分别用 audio-only、video-only 文件验证；输出包含 `has_audio`、`has_video`、duration、codec、width/height/fps。 | `/goal 实现 video-summary inspect <input-file> --output <dir>，用 ffprobe 生成 metadata.json，并支持 audio-only 和 video-only 文件。` |
| 3 | 建立输出目录规则和配置加载：默认 `outputs/<文件名>/`，支持 `--output`，默认不覆盖。 | 输出路径稳定；重复运行时有明确提示；config 默认值可被测试读取。 | `/goal 添加输出目录管理和 config.yaml 支持，默认不覆盖已有输出，提供清晰错误信息和路径生成测试。` |
| 4 | 实现音频提取：有音频流时用 `ffmpeg` 输出 `audio.wav`；无音频时写状态并跳过。 | audio-only 文件能生成 wav；video-only 文件不崩溃，并在日志/metadata 中说明无音频。 | `/goal 实现音频提取步骤：有音频流则生成 audio.wav，无音频流则跳过并记录状态，不做转录。` |
| 5 | 实现 Whisper 转录：从 `audio.wav` 生成 `transcript.json` 和 `transcript.srt`。 | 选一个短音频样例跑通；无音频输入生成空 transcript 或明确跳过。 | `/goal 接入 faster-whisper，把 audio.wav 转成 transcript.json 和 transcript.srt；无音频时输出空转录结果并不中断。` |
| 6 | 实现固定间隔抽帧：有视频流时生成 `frames/frame_000001.jpg` 等文件，控制长边尺寸。 | video-only 文件能抽帧；audio-only 文件跳过；帧数量符合间隔配置。 | `/goal 实现 ffmpeg 固定间隔抽帧，支持 --frame-interval 和 max_frame_width；无视频流时跳过。` |
| 7 | 实现 frame metadata：记录每张帧的 `frame_id`、timestamp、image_path。 | 抽帧后生成稳定 JSON；timestamp 与文件序号对应。 | `/goal 为抽帧结果生成 frame metadata JSON，包含 frame_id、timestamp 和相对图片路径，并添加测试。` |
| 8 | 接入 OCR 可选模块：PaddleOCR 可用时生成 `ocr.json`；不可用时明确降级。 | PaddleOCR 缺失不会导致整条 pipeline 失败；有 OCR 时结构稳定。 | `/goal 封装 OCR 步骤，优先使用 PaddleOCR；如果依赖缺失或初始化失败，输出可解释的跳过状态，不中断 pipeline。` |
| 9 | 实现 OCR 去重：相邻帧文本归一化和相似度去重。 | 单元测试覆盖空文本、重复文本、轻微空格/标点差异。 | `/goal 实现 OCR 文本归一化和相邻帧去重，输出 is_duplicate 和 duplicate_of 字段，并添加单元测试。` |
| 10 | 实现 `timeline_events.json`：按 30-60 秒窗口合并 transcript、OCR、frame metadata。 | audio-only、video-only、音视频都有的输入都能生成 timeline；缺失来源为空而不是报错。 | `/goal 实现 timeline builder，按固定时间窗口合并 transcript、OCR 和 frame metadata，生成 timeline_events.json。` |
| 11 | 接入 GPT 文本总结：读取 timeline，调用 OpenAI 文本模型生成 `final_summary.md`。 | 无 API key 时给出明确错误；有 API key 时生成 Markdown；不发送原始视频，只发送结构化文本证据。 | `/goal 接入 OpenAI 文本总结，只基于 timeline_events.json 生成 final_summary.md；处理缺少 OPENAI_API_KEY 的情况。` |
| 12 | 串联 MVP pipeline：`video-summary run <input-file>` 一次跑完 inspect、音频、转录、抽帧、OCR、timeline、summary。 | 对一个短样例完整跑通；生成 `metadata.json`、可用的中间产物、`timeline_events.json`、`final_summary.md`。 | `/goal 串联 MVP pipeline，提供 video-summary run <input-file> --output <dir>，按顺序执行已实现步骤并写日志。` |
| 13 | 增加最小回归样例和验收脚本：固定 2-3 个本地样例，不提交视频本体。 | `pytest` 通过；验收命令能检查输出文件存在和 JSON schema 基本正确。 | `/goal 添加 MVP 回归测试和验收命令，验证输出文件存在、JSON 可解析、audio-only/video-only 不崩溃。` |
| 14 | 整理 README 使用说明：安装、运行、输出目录、已知限制、下一阶段。 | README 命令可复制运行；不夸大未实现功能。 | `/goal 更新 README，写清安装、inspect、run、输出文件、已知限制和下一阶段计划。` |

## 后续阶段

| 阶段 | 开发目标 | 验收方式 | 推荐指令 |
|---|---|---|---|
| 15 | PySceneDetect 场景切分，保留固定间隔抽帧作为 fallback。 | `scenes.json` 生成；场景检测失败时仍可用固定抽帧。 | `/goal 添加 PySceneDetect 场景切分，生成 scenes.json，并保留固定间隔抽帧 fallback。` |
| 16 | 场景关键帧选择和图片去重。 | 每个场景选 1-3 张关键帧；相似帧被过滤。 | `/goal 实现场景关键帧选择和图片相似度去重，生成 scene_keyframes.json。` |
| 17 | GPT Vision 分析关键帧，带成本控制。 | 限制 `max_frames` 和图片尺寸；生成 `frame_descriptions.json`。 | `/goal 接入 GPT Vision 分析关键帧，添加 max_frames、max_image_width 和跳过重复帧控制。` |
| 18 | 分层总结：片段总结、章节总结、最终总结。 | 长视频不会一次性塞给模型；输出 `chapter_summaries.md` 和 `timeline_summary.md`。 | `/goal 实现分层总结：先总结时间片段，再生成章节总结和最终总结。` |
| 19 | HTML 报告。 | `report.html` 展示时间线、转录片段、OCR、关键帧、总结。 | `/goal 添加 report.html 生成，不做 Web 服务，只生成本地静态报告。` |
| 20 | Batch 模式。 | 目录内多个视频可逐个处理；单个失败不影响其它文件。 | `/goal 添加 batch 模式，处理目录内多个媒体文件，输出独立结果目录和汇总状态。` |
| 21 | Agent 封装。 | 只包装稳定 pipeline 工具，不让 agent 随机改变流程。 | `/goal 在稳定 pipeline 基础上封装 agent tools，但保持确定性执行顺序和可复现日志。` |

## 统一验收命令

第一阶段每个目标完成后建议至少跑：

```powershell
git status --short
python -m pytest
python -m video_summarizer --help
```

从第 2 步开始，增加：

```powershell
python -m video_summarizer inspect "video\<一个样例文件>.mp4" --output outputs\inspect-demo
```

从第 12 步开始，增加：

```powershell
python -m video_summarizer run "video\<一个短样例文件>.mp4" --output outputs\mvp-demo
```

如果 CLI 后续安装了 console script，也可以改用：

```powershell
video-summary inspect "video\<一个样例文件>.mp4" --output outputs\inspect-demo
video-summary run "video\<一个短样例文件>.mp4" --output outputs\mvp-demo
```

## 明确默认和边界

- 默认只分析本地文件，不下载、不破解接口、不绕过验证码、不调用非公开 API。
- `video/` 只作为本地测试素材，不进入 Git。
- 当前素材存在 audio-only/video-only 文件；MVP 必须把缺失音频或缺失视频当作正常输入形态处理。
- OCR 和 GPT 都放在后面接入；前面的 ffprobe、ffmpeg、timeline 必须先稳定。
- 第一阶段不做 Web UI、多 agent、向量搜索、自动剪辑、逐帧视觉理解。
- 每个 `/goal` 完成后都要汇报：改了什么、如何验证、哪些限制仍然存在。
